<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sv.com.kubobank.identity.infrastructure.persistence.CustomerMapper">
    <insert id="insert">
        INSERT INTO customers (
        customer_id,
        full_name,
        email,
        phone_country_code,
        phone_number,
        status,
        created_at,
        updated_at
        )
        VALUES (
        #{customerId},
        #{fullName},
        #{email},
        #{phoneNumber.countryCode},
        #{phoneNumber.number},
        #{status},
        #{createdAt},
        #{updatedAt}
        )
    </insert>
</mapper>
