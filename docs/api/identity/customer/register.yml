info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/customer
  body:
    type: json
    data: |-
      {
        "fullName": "Irma Quijano",
        "email": "irma.quijano@gmail.com",
        "phoneNumber": {
          "number": "75830425",
          "countryCode": "+503"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
